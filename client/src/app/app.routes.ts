import { Routes } from '@angular/router';
import { MapViewportComponent } from './components/map-viewport-component/map-viewport-component';

export const routes: Routes = [
  { path: '', component: MapViewportComponent, title: 'Map Viewer' },
];
